- id: scripted-answers
  config:
    default:
      custom: 'Multi-user with accounts and cloud sync.'
    recordPath: '__DOGFOOD_RECORD_PATH__'
- insert:
    - id: align-driver
      name: 'dsh-requirements-alignment/align-driver'
      config:
        recordPath: '__DOGFOOD_RECORD_PATH__'
